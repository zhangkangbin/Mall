<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zkb.mall">

    <application
        android:name="com.z.baselibrary.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Base">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.order.OrderConfirmationActivity"
            android:label="@string/title_activity_order_confirmation"
            android:theme="@style/AppTheme.Base" />
        <activity android:name=".ui.order.PayCompleteActivity"></activity>
    </application>

</manifest>